## 🚀 What's New
- Added support for new data format in `output.py`.

## 🔧 Improvements
- Optimized performance of data processing in `output.py`.
- Enhanced error handling to provide more informative messages.

## 🐛 Bug Fixes
- Fixed an issue where `output.py` could crash when encountering empty input.
- Resolved a formatting error that caused incorrect output.

## ⚠️ Breaking Changes
- The function signature for `process_data` in `output.py` has changed. Please update your code accordingly.