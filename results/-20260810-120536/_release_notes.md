## 🚀 What's New
- Added support for new data format in `output.py`.

## 🔧 Improvements
- Optimized performance of data processing in `output.py`.
- Enhanced error handling to provide more informative messages.

## 🐛 Bug Fixes
- Fixed an issue where `output.py` could crash when processing certain types of input.
- Resolved a formatting error that caused incorrect output in reports generated by `output.py`.

## ⚠️ Breaking Changes
- Removed deprecated function `old_function()` in favor of `new_function()`.