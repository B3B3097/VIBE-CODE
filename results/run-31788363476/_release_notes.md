## 🚀 What's New
- Added support for new output format in `output.py`.

## 🔧 Improvements
- Optimized performance of data processing in `output.py`.

## 🐛 Bug Fixes
- Fixed an issue where `output.py` could crash when processing large datasets.

## ⚠️ Breaking Changes
- The `output.py` function now requires a new parameter `format_type`.